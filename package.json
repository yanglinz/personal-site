{
  "name": "personal-site",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:parcel": "parcel src/index.js",
    "dev:hugo": "hugo serve -D",
    "start": "run-p dev:*",
    "build:parcel": "parcel build -d assets/build src/index.js",
    "build:hugo": "hugo",
    "build": "run-s build:parcel build:hugo",
    "format": "prettier --write '*.{js,md}' '**/*.{js,scss,css,md}'"
  },
  "dependencies": {
    "lodash": "^4.17.10",
    "normalize.css": "^8.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.10.0",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.10.3",
    "prettier": "^1.15.2"
  }
}
