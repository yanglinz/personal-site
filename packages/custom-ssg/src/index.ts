import { Path, ContentManifest } from './types';

/**
 * Generate a list of content manifests.
 */
export function getContentTree(inputDirectory: Path): ContentManifest[] {
  return [];
}

/**
 * Generate files (mainly html and images) from a content manifest.
 */
export function renderContent(content: ContentManifest) {
  // generate file(s) given 
}
