{
  "name": "personal-site",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "./scripts/build.sh",
    "format": "prettier --write '*.{js,md}' 'packages/**/src/**/*.{js,scss,css,md}'"
  },
  "devDependencies": {
    "lerna": "^2.9.0"
  },
  "dependencies": {
    "prettier": "^1.12.1"
  }
}
