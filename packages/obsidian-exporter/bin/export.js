const path = require('node:path');
const fsp = require('node:fs/promises');

async function getManifest() {
    const postsPath = path.resolve(process.cwd(), '../../..');
    
}

async function exportPost() {
  
}

function exportAll() {
    // get a list of posts
    // export each post
}

exportAll();
